{
    // Configurações específicas do projeto para a extensão Cucumber (Gherkin)
    "cucumber.features": [
        // Caminho relativo à raiz do projeto para seus arquivos .feature
        "Scenarios/**/*.feature"
    ],
    "cucumber.glue": [
        // Caminho relativo à raiz do projeto para seus arquivos de steps (.js)
        "Scripts/Page/**/*.js"
    ]
}